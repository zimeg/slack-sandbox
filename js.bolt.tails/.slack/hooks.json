{
  "hooks": {
    "deploy": "systemctl --user --machine=ez@.host restart slack:tails.service",
    "get-hooks": "npx -q --no-install -p @slack/bolt slack-cli-get-hooks",
    "start": "npm run watch"
  }
}
