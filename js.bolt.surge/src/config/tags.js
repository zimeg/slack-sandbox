/**
 * @readonly
 * @enum {string} Configuration states for the program
 * @property {string} DEVELOPMENT "development"
 * @property {string} PRODUCTION "production"
 */
const tags = {
  DEVELOPMENT: "development",
  PRODUCTION: "production",
};

export default {
  ...tags,
};
